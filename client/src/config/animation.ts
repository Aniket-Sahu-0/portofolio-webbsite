// Shared animation/cadence config for sliders and galleries
// Update values here to keep site-wide timing in sync.

export const SLIDE_DURATION_MS = 5000; // 5s per slide
export const TRANSITION_DURATION_S = 0.8; // 0.8s crossfade/slide
export const EASE_CURVE: [number, number, number, number] = [0.22, 1, 0.36, 1];
